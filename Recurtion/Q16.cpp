#include <iostream>
#include <vector>
using namespace std;

bool isPalindrome(string& s, int left, int right) {

}

int main() {

    string s = "aab";

    vector<string> current;

    partition(s, 0, current);

    return 0;
}